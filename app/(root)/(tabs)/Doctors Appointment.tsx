import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const DoctorsAppointment = () => {
  return (
    <SafeAreaView>
      <Text>DoctorsAppointment</Text>
    </SafeAreaView>
  );
};

export default DoctorsAppointment;
